- title "Preguntas y respuestas sobre bádminton"
- keywords "Preguntas y respuestas sobre bádminton"
- head_desc "Aquí hay una colección de habilidades, tácticas y precauciones para todos los aspectos del bádminton. Se actualiza diariamente. Los entusiastas del bádminton pueden ver y dejar mensajes."
- path("/es/answer/")
- mobile_url("http://www.vxixi.com/es/answer/")

%section.container
  .row
    %article.col-md-10
      %h1.title.hide Preguntas y respuestas sobre bádminton
      - @articles.each do |article|
        .row
          .col-md-12
            %a{href: "/es/answer/#{article.id}/", title: "#{article.title}"}
              %h2.title #{article.title}
      #toTop ^
      .page-header
      #SOHUCS
      .page-header

    %aside
      = render partial: "layouts/ad_right_es" 
