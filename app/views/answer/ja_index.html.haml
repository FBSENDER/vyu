- title "バドミントン Q&A"
- keywords "バドミントン Q&A"
- head_desc "バドミントンのあらゆるスキル、戦術、注意点をまとめました。 毎日更新されます。 バドミントン愛好家の方はぜひご覧いただき、メッセージを残してください。"
- path("/ja/answer/")
- mobile_url("http://www.vxixi.com/ja/answer/")

%section.container
  .row
    %article.col-md-10
      %h1.title.hide バドミントン Q&A
      - @articles.each do |article|
        .row
          .col-md-12
            %a{href: "/ja/answer/#{article.id}/", title: "#{article.title}"}
              %h2.title #{article.title}
      #toTop ^
      .page-header
      #SOHUCS
      .page-header

    %aside
      = render partial: "layouts/ad_right_ja" 
